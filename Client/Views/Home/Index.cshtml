
@{
    ViewBag.Title = "Home";
}

<h2>@ViewBag.Title</h2>

@if (CurrentUser.HasRole("Admin"))
{
    <button type="button" class="btn btn-primary" onclick="location.href='@Url.Action("Index", "Admin")'">User Table</button>
}